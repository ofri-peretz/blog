{
  "slug": "burgee-change-one-import",
  "stage": "review",
  "panel_version": "1.0.0",
  "spec": "sdlc/spec/burgee-change-one-import.md",
  "reviewed": "2026-09-23",
  "round": 5,
  "findings_fixed_this_round": [
    "Re-pin, not a finding: both 0.9.2 defects were fixed upstream and released as burgee@0.11.1 (2026-09-23T13:52Z). The whole tutorial was re-run on 0.11.1: every probe, a stranger run from an empty directory (7/7 parity, all transcripts byte-exact), and the compat oracle at the burgee@0.11.1 tag (1360 / 1360, control 1360 / 1360). The article and all 43 spec rows are now pinned to 0.11.1, and the burgee rows read the tag instead of origin/main.",
    "The defects section became 'Two defects, fixed in 0.11.1'. It keeps the 0.9.2 finding and shows the fixed MCP call returning `lines: 4`. The docs-drift caveat was dropped because 0.11.1's agent-surfaces.mdx documents the running behaviour. Kept: the by-design `--json` null narrowing and its one-line fix; the printed line reaching the MCP stream (the upstream fix is not released); and the uncaught throw under `--json`.",
    "Round 4, BLOCKING, compatibility: the new Node 20 support applies only once commander leaves the reader's dependencies (commander 15 declares >=22.12.0, and yarn 1 on Node 20.19.5 refuses it). The sentence now says so.",
    "Round 4, BLOCKING, reproducibility: `BURGEE_VERSION=0.9.2 … all` crashed in the re-pinned multi-word probe, so the article's 0.9.2 history could not be replayed. The probe now prints the refusal on 0.9.2 (`unknown option '--skipBlank'`), and the replay completes with only the two defect probes differing.",
    "Node: the 0.9.2 claim 'rises to >=24; yarn 1 refuses on Node 22' no longer applies. It was replaced by the 0.11.1 range `^20.19.0 || >=22.13.0`, with new probes showing the swapped file runs on Node 20.19.5 and 22.22.0.",
    "Voice: the defects are owned in the first person ('I hit two defects', 'I fixed both in 0.11.1'). Two internal links were added on existing words: to the I-27 stack article (on 'five sibling packages') and to the input-schema article (on 'a JSON Schema per command').",
    "Correctness wording: 'the declared Node range goes from' replaces '`engines.node` goes from', because the reader's own engines field does not change. 'unmodified apart from the import' was shortened to 'unmodified', since the oracle shims imports rather than editing files."
  ],
  "checked_and_found_correct": [
    "On 0.11.1, every transcript was re-run byte-for-byte by three reviewers, including one fresh correctness reviewer who had not seen earlier drafts. The program block is byte-identical to the repro's file, and the second-edit excerpt matches `diff -u cli.js cli.answering.js`.",
    "The null envelope traced to source at the tag: `command.js` writes `data: value ?? null`, and commander actions return nothing. Under `--mcp`, only the façade's own streams are captured, so a bare `console.log` still reaches the JSON-RPC stream on 0.11.1. The 0.11.1 CHANGELOG lists both defect fixes.",
    "All 43 spec rows pass the rebased detector's rules (a single backtick span, `set -u`, 60s timeout). The real `detect-stale-claims.mjs` reports no findings for this slug.",
    "Stranger path from an empty directory with the exact bash block gives 7/7 parity. After `npm uninstall commander`, the swapped file still runs on Node 24 and on 20.19.5.",
    "Install matrix on 0.11.1: npm, yarn, pnpm and bun each install and run it. `require()` works. TypeScript's legacy node resolver still gives TS2307, and bundler and nodenext give 0 errors. All six packages declare `^20.19.0 || >=22.13.0`.",
    "Sizes: commander is 207,368 bytes, and burgee 0.11.1 plus its five siblings is 1,266,628 bytes, matching the installed versions. At the tag, the README's `lighter-than-commander` gate reads 1.514×, not met.",
    "Framing: no banned vocabulary. commander is named as the smaller choice when no agent calls the CLI.",
    "devto-review round 4 had zero blockers from eight of nine reviewers (Technical 9.6, Quality 9.5, Voice 9.5, Checklist 10.0, gate score 9.2). The one blocker was OVERRIDDEN with evidence. Linkability marked the new link to burgee-zero-dependency-cli-stack as unmatched because its ground-truth manifest (eslint apps/docs/src/data/articles.json) predates that article. The article was published hours earlier: devto_id 4722817, the dev.to URL returns 200, and the ofriperetz.dev page returns 200 with the matching title. Refreshing that manifest belongs to the post-publish step in another repo."
  ],
  "lenses": {
    "growth_hook": {
      "score": 9.6,
      "why": "The title still delivers what the body shows on 0.11.1: an untouched `count --json` still ends in `\"data\":null`. The thesis line opens paragraph 2, so the opening works on its own. The re-pin strengthens the arc from confession to closed loop: the reader sees the same MCP call that was refused on 0.9.2 now return `lines: 4`. The closing question is specific enough to draw war stories. Held below 10 by the frozen chassis' `getting-started` label, which undersells a piece written for maintainers."
    },
    "security_correctness": {
      "score": 9.6,
      "why": "A reviewer who had not seen earlier drafts re-derived every claim against the 0.11.1 tarball and tag source: every transcript byte-exact, and each causal explanation matched to the façade code. That covers the null envelope, the print still reaching the MCP stream, the fixed flag mapping, the exit-code split, and the absence of a public 'machine asked?' flag. The two loose phrases it found (the engines wording, 'unmodified apart from the import') were tightened."
    },
    "structure_framing_voice": {
      "score": 9.6,
      "why": "Every H2 still serves one argument. The defects section now pays off the keep-versus-gain line with a fix shown working, not just asserted. The tutorial runs end to end with every file named before use. Framing stays landscape: burgee's unmet gate, the `.effects` lock-in, and commander as the smaller choice are all still on the page. Defect ownership moved into the first person, which is this corpus's register."
    },
    "compatibility": {
      "score": 9.6,
      "why": "The matrix was re-verified on 0.11.1: four package managers, `require()`, TypeScript by resolver, and Node 20 and 22. The new Node 20 claim is now correctly conditional on commander leaving dependencies. Frontmatter, `reading_time_minutes`, slug-derived cover stems and every outbound or internal link check out. Live covers will 404 until the site deploys this branch; the owner's dispatch should follow the deploy."
    },
    "reproducibility": {
      "score": 9.6,
      "why": "All 43 rows re-ran green under the rebased detector's rules. A stranger following the article from an empty directory reproduces every transcript. The oracle was re-run at the tag, not quoted. The 0.9.2 history is now replayable with one environment variable. The remaining gap is house-wide: seven rows need a burgee checkout with tags and Node 20/22 binaries on the weekly runner."
    }
  },
  "min": 9.6,
  "verdict": "pass",
  "notes": [
    "Growth, structure, and the fresh correctness reviewer scored the round-4 text. Compatibility and reproducibility scored round 5. Round 5's only later edits were the correctness reviewer's two wording nits, both word-neutral or shorter, neither adding a number.",
    "Still open upstream, and kept in the article: a printing action's output reaches stdout during an MCP tool call (fix in progress, not released), and a throw inside an action stays uncaught under `--json`.",
    "Earlier-round burgee findings, fixed in 0.11.1 per the fix lane: MCP multi-word option mapping, completions offering refused flags, the missing bin shebang, the README quickstart, and the MCP docs."
  ],
  "earlier_rounds_fixed": [
    "Round 1, BLOCKING, reproducibility: a stranger could not follow the tutorial. `sample.txt` was never given and `cli.commander.js`, `cli.js` and `cli.answering.js` appeared only inside commands. The program block is now named, and the install block writes `sample.txt` with a `printf` that is byte-identical to the repro's fixture.",
    "Round 1, BLOCKING, reproducibility: the `pm-matrix` spec row took 82s on a cold cache, over the stage-6 detector's 60s `execSync` limit. It is now split into `pm-yarn`, `pm-pnpm` and `pm-bun`, each installing into a wiped directory under a 50s install timeout.",
    "Round 1, BLOCKING, structure: the same missing file roles, raised independently from the voice side.",
    "Round 1, BLOCKING, growth: the opening had no line that worked on its own. 'One import describes the CLI to an agent; answering takes a `return`.' now opens paragraph 2.",
    "Round 1, BLOCKING, compatibility: the Node floor rise (commander `>=22.12.0` to burgee `>=24`) was stated without its consequence. On Node 22.22.0, yarn 1 refuses the install (probe `yarn1-node22-exit`). The article now calls it a major for your users.",
    "Round 1, BLOCKING, compatibility: TypeScript was not mentioned, and `burgee/commander` is an `exports`-only subpath, so the legacy `node` resolver fails with TS2307 where `commander` resolves. The article now says so (probe `ts-node10-ts2307`). `bundler` and `nodenext` compile with zero errors of any kind.",
    "Round 2, BLOCKING, reproducibility: from an empty directory the install block did not reproduce. commander was not installed, and without `\"type\": \"module\"` Node 24 printed a warning that made stderr differ on the parity argv. The block now installs `commander@15.0.0 burgee@0.9.2` and runs `npm pkg set type=module`. Re-run from an empty directory, all seven argv match on stdout, stderr and exit code.",
    "Pinned the install to `burgee@0.9.2`. `burgee@0.10.0` reached npm `latest` at 05:34Z while the panel ran. Every behavioural probe returns the same on 0.10.0 (`BURGEE_VERSION=0.10.0 node sdlc/spec/burgee-change-one-import.repro.mjs all`), but the size row would move, so the pin stays and the spec records the re-pin path.",
    "correctness: 'this swap buys you nothing' overstated the case, since completions help human users too. That clause is gone. 'Static scripts' became 'static output', because fig emits a JSON spec. The size section now separates on-disk bytes (207,368 vs 1,188,994) from the bundle ratio (1.514×, not met), which a reader could otherwise have read as contradicting each other.",
    "The anchor 'commander's own suite' pointed at an ESLint article. It now points at commander's `v15.0.0/tests`, the tag the oracle vendors.",
    "Tags: `cli` swapped for `webdev`. The 9-reviewer devto panel flagged `cli` as unmeasured and `webdev` as the broad tag with measured returns."
  ]
}
